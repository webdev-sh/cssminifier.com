extends layout

block content

  h3 API

  p.
    To minify/compress your CSS perform a POST request to <code>#{env.baseUrl}/raw</code> with the
    <code>input</code> parameter set to the CSS you want to minify.

  h4 wget
  pre.
    wget --post-data='input=p { color : red; }' --output-document=- #{env.baseUrl}/raw
    wget --post-data="input=`cat style.css`" --output-document=style.min.css #{env.baseUrl}/raw

  h4 curl
  pre.
    curl -X POST -s --data-urlencode 'input=p { color : red; }' #{env.baseUrl}/raw
    curl -X POST -s --data-urlencode 'input@style.css' #{env.baseUrl}/raw > style.min.css

  h4 POST
  pre.
    POST #{env.baseUrl}/raw?input=...

  p
    | To see implementations in other languages see the other Language Examples pages below.

  include ./include/languages.pug

