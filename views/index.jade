extends layout

block content
  div.row
    div.span6
      h3.muted Input CSS
      form#input-form.form-stacked(method="post", name="f", action="/minify")
        fieldset
          div.clearfix
            div.input
              textarea#input.span6(name="input",rows="15",wrap="off",autofocus="autofocus",spellcheck="false")
          div.actions
            button#minify.btn.btn-primary(type="submit") Minify
            |  
            button#download.btn(type="button",disabled="disabled") Download as File
            |  
            button#raw.btn(type="button",disabled="disabled") RAW
            |  
            input#clear.btn(type="reset",value="Clear")

    div.span6
      h3.muted Minified Output
      form#output-form.form-stacked(name="f")
        fieldset
          div.clearfix
            div.input
              textarea#output.span6(name="output",rows="15",wrap="soft",disabled="disabled")
          div.actions
            button#select.btn(type="button",disabled="disabled") Select All

  h2.muted API

  h3.muted General
  p.
    To minify your CSS perform a POST request to <code>http://cssminifier.com/raw</code> with the <code>input</code>
    parameter set to the CSS you want to minify.
  pre.
    POST http://cssminifier.com/raw?input=...
  p
    | To see implementations in other languages see the 
    a(href="/examples" ) Examples
    |  page.

  p (Ends)
