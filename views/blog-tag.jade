extends blog

block blogcontent

  h2 Posts tagged with &apos;#{thisTagName}&apos;

  ul
    for post in thesePosts
      li #{post.meta.moment.format("DD MMM YYYY")} - 
        a(href="./#{post.name}")= post.meta.title
